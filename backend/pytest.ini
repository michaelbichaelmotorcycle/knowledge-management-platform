[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    integration: requires a live Postgres+pgvector instance
    e2e: end-to-end smoke tests across the full pipeline